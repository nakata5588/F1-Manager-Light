// Aplica regras/penalizações leves por dia (placeholder)
export function applyRulesTick(gs) {
  const next = { ...gs };

  // Exemplo: verificar penalizações de grid guardadas em rules / events (no futuro)
  // Neste stub não alteramos nada — serve só como “gancho”.
  return next;
}
